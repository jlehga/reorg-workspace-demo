════════════════════════════════════════════════════════════════
TRY THE DEMO (no install required)
════════════════════════════════════════════════════════════════
1. Live demo (primary — open this first):
   https://jlehga-reorg-workspace-demo-app-ggbknu.streamlit.app/

   Demo login:
     Username: demouser
     Password: test123

2. Design deliverable: docs/design.md

3. Source (version-controlled; not required to review):
   Private repo: https://github.com/jlehga/reorg-execution-system
   Access on request, or use an attached / release zip. Cloning is optional.

4. OPTIONAL — only if the live link does not work:
   Zip (preferred fallback — no clone):
     Download reorg-execution-system.zip from GitHub Releases:
       https://github.com/jlehga/reorg-execution-system/releases
     Or build locally: ./make_zip.sh  →  dist/reorg-execution-system.zip
     Then: unzip → python -m venv .venv → source .venv/bin/activate
       → pip install -r requirements.txt
       → streamlit run app.py --server.port 8765
       → http://127.0.0.1:8765

   Clone & run (only if access has been granted):
     git clone https://github.com/jlehga/reorg-execution-system.git
     cd reorg-execution-system
     python -m venv .venv
     source .venv/bin/activate          # Windows: .venv\Scripts\activate
     pip install -r requirements.txt
     streamlit run app.py --server.port 8765
     Or: ./run_demo.sh
     Open: http://127.0.0.1:8765

Hosting notes: docs/HOSTING.md

After Sign in you land on Workspace home. Use sidebar Sign out to return
to the login screen.

LLM_API_KEY is optional. Without it, a deterministic demo extractor runs
(fine for the walkthrough). With a key, interpretation uses a live LLM.
Optional: LLM_MODEL, LLM_BASE_URL.

════════════════════════════════════════════════════════════════
60-SECOND DEMO PATH
════════════════════════════════════════════════════════════════
1. Sign in with demouser / test123
2. Workspace home → New Reorg Case (or Start from scenario / Scenario A)
3. Analyze Reorg
4. Reorg Case: note unverified Finance approval warning
5. Plan & Approve → Approve Plan
6. Execution → Run execution → Mark Complete on GL task (CC-4175)
7. Reconciliation: all Passed → case Completed
8. Back to workspace to see the case in Past cases
9. (Optional) Audit tab for the event trail

Optional failure beat: before Mark Complete, check "Simulate incorrect GL
entry" → reconciliation fails / reporting blocked.
Optional Scenario B: load Scenario B for ambiguity / validation failures.

Design: docs/design.md
Tests:  pytest -q
